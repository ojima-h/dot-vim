dot-vim
=======

Install
-------

    $ git clone git@github.com:ojima-h/dot-vim.git .vim
    $ cd .vim
    $ git submodule init
    $ git submodule update
    $ cat <EOF > ~/.vimrc
    source ~/.vim/init.vim
    EOF
